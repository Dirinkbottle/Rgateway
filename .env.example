# ─── Rgateway 配置文件 ───
# 复制此文件为 .env 并修改，或直接设置系统环境变量

# Go 业务后端地址
BACKEND_URL=http://127.0.0.1:8080

# 对外服务端口（浏览器访问）
PUBLIC_PORT=3000

# 管理端口（仅 127.0.0.1，供后端调用失效缓存）
ADMIN_PORT=3001

# 默认缓存 TTL，单位秒，259200 = 3 天
CACHE_TTL_SECS=259200

# 最大缓存条目数，超出后 LRU 淘汰
CACHE_MAX_ENTRIES=10000
